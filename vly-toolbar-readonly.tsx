/**
 * Vly toolbar stub.
 *
 * In the Freebuff preview environment this file is injected and provides the
 * live toolbar. Outside that environment (e.g. building locally or on GitHub
 * Actions) this no-op stub keeps the import in src/main.tsx resolving so the
 * production build succeeds. It renders nothing.
 */
export function VlyToolbar() {
  return null;
}

export default VlyToolbar;
